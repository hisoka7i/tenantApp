
/etc/nginx/sites-available/
sudo apt update
sudo apt install node
sudo apt intall npm
sudo apt install nginx
sudo systemctl enable nginx
systemctl status nginx
cd/etc/init.d/nginx restart

//--- clone the project
git clone --filter=blob:none --no-checkout https://github.com/hisoka7i/tenantApp.git
cd tenantApp/
git sparse-checkout set --cone
git checkout main
git sparse-checkout set tenantFrontEnd


... Very Important
instead for doing npm run build in aws ec2. Do it in local machine and then run it on server.

copy the build folder in sudo cp -r /build /var/www/html/


server {
    listen 80;
    listen [::]:80;
    server_name example.com;

    root /home/ubuntu/tenantApp/tenantFrontEnd/my-tenant-app/build;

    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
